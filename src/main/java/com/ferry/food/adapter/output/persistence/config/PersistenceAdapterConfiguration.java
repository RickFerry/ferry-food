package com.ferry.food.adapter.output.persistence.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for persistence layer adapters.
 * 
 * Note: Repository adapter implementations are marked with @Component annotations
 * in the adapter layer and are automatically detected and registered by Spring's component scanning.
 * 
 * This class is kept as a placeholder for future configuration needs and serves as
 * documentation for the persistence adapter architecture.
 */
@Configuration
public class PersistenceAdapterConfiguration {
    // Repository adapter beans are automatically scanned from @Component annotations
}
