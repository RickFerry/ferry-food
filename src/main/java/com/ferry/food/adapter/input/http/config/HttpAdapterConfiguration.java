package com.ferry.food.adapter.input.http.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for HTTP input adapters.
 * This class is a placeholder for HTTP-related Spring beans such as:
 * - Response mappers
 * - HTTP validators
 * - Error handlers
 * - Custom HTTP converters
 *
 * Currently, this configuration is minimal as most HTTP concerns are handled
 * by the REST controllers and global exception advice.
 */
@Configuration
public class HttpAdapterConfiguration {
    // HTTP adapter beans can be added here as needed
}
